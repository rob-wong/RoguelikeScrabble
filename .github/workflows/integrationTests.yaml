name: Android CI (Production)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  instrumentation-tests:
    runs-on: macos-latest # This is an Intel runner, so we must use an Intel emulator.

    steps:
      - name: 🧩 Checkout
        uses: actions/checkout@v4

      - name: ☕ Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🧰 Install dependencies
        run: |
          brew install coreutils || true
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg

      - name: ⚙️ Set up Android SDK
        # This ENV var fixes the JAXB error in modern JDKs for sdkmanager
        env:
          _JAVA_OPTIONS: "-XX:+IgnoreUnrecognizedVMOptions --add-modules java.xml.bind,java.xml.ws.annotation"
        run: |
          echo "--- Preparing Android SDK ---"
          # Using a known stable version of cmdline-tools
          export CMDLINE_TOOLS_URL="https://dl.google.com/android/repository/commandlinetools-mac-10406996_latest.zip"
          mkdir -p "$ANDROID_SDK_ROOT/cmdline-tools"
          curl -sSL -o "$ANDROID_SDK_ROOT/cmdline-tools/tools.zip" "$CMDLINE_TOOLS_URL"
          unzip -q "$ANDROID_SDK_ROOT/cmdline-tools/tools.zip" -d "$ANDROID_SDK_ROOT/cmdline-tools"
          mv "$ANDROID_SDK_ROOT/cmdline-tools/cmdline-tools" "$ANDROID_SDK_ROOT/cmdline-tools/latest"
          rm "$ANDROID_SDK_ROOT/cmdline-tools/tools.zip"

          # Add SDK tools to path for all subsequent steps
          echo "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" >> $GITHUB_PATH
          echo "$ANDROID_SDK_ROOT/emulator" >> $GITHUB_PATH
          echo "$ANDROID_SDK_ROOT/platform-tools" >> $GITHUB_PATH

          echo "--- Accepting licenses ---"
          yes | sdkmanager --licenses

          # ✅ THE CRITICAL CHANGE: Switched from 'arm64-v8a' to 'x86_64' to match the Intel runner
          echo "--- Installing Intel (x86_64) emulator components ---"
          sdkmanager --update
          sdkmanager "platform-tools" "emulator" "platforms;android-30" "system-images;android-30;google_apis;x86_64"

      - name: 📱 Create Intel AVD
        run: |
          # ✅ Use the x86_64 package to create the AVD
          echo "--- Creating x86_64 AVD ---"
          echo "no" | avdmanager create avd -n test --package "system-images;android-30;google_apis;x86_64" --force

          # Patch the AVD config for stability in CI
          echo "--- Patching AVD config for CI ---"
          echo "hw.cpu.ncore=2" >> ~/.android/avd/test.avd/config.ini
          echo "hw.ramSize=2048" >> ~/.android/avd/test.avd/config.ini
          echo "hw.gpu.enabled=yes" >> ~/.android/avd/test.avd/config.ini
          echo "hw.gpu.mode=auto" >> ~/.android/avd/test.avd/config.ini

      - name: 🚀 Launch Android Emulator
        run: |
          echo "--- Starting emulator with hardware acceleration ---"
          # Hardware acceleration will now work because the architectures match
          nohup $ANDROID_SDK_ROOT/emulator/emulator \
            -avd test \
            -no-window \
            -no-snapshot \
            -noaudio \
            -no-boot-anim \
            -verbose > emulator-log.txt 2>&1 &
          
          sleep 15
          
          echo "--- Waiting up to 10 minutes for emulator to boot... ---"
          boot_completed=""
          n=0
          # Use gtimeout for a clean exit after 10 minutes
          gtimeout 600 bash -c '
            while [ "$(adb shell getprop sys.boot_completed 2>/dev/null)" != "1" ]; do
              echo "[$(date +"%H:%M:%S")] Boot status: `adb shell getprop sys.boot_completed 2>/dev/null`"
              adb devices
              sleep 10
            done
          '
          
          # Final check
          boot_completed=$(adb shell getprop sys.boot_completed 2>/dev/null | tr -d '\r')
          if [ "$boot_completed" != "1" ]; then
            echo "❌ Emulator failed to boot within 10 minutes."
            echo "=== EMULATOR LOG DUMP ==="
            cat emulator-log.txt || echo "No emulator log found."
            exit 1
          fi

          echo "✅ Emulator booted successfully!"
          adb shell settings put global window_animation_scale 0
          adb shell settings put global transition_animation_scale 0
          adb shell settings put global animator_duration_scale 0

      - name: 🧪 Run Instrumentation Tests
        run: ./gradlew connectedCheck --stacktrace

      - name: 📤 Upload Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: instrumentation-test-reports
          path: app/build/reports/androidTests/connected/
